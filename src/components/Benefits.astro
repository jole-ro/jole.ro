---
import WhyChooseJolero from "./WhyChooseJolero.astro";
const { lang = "en" } = Astro.props;

const content = {
  en: {
    subtitle:
      "🔹 With over 10 years of expertise in Local SEO and Trust Signals, we help businesses get found and grow through strategic lead generation.",
  },
  hu: {
    subtitle:
      "🔹 Több mint 10 éves tapasztalattal a helyi SEO és a bizalmi jelek terén, segítünk a vállalkozásoknak a növekedésben és az ügyfélszerzésben.",
  },
  ro: {
    subtitle:
      "🔹 Cu peste 10 ani de expertiză în SEO Local și Semnale de Încredere, ajutăm afacerile să fie găsite și să crească prin strategii de lead generation.",
  },
};

const t = content[lang as keyof typeof content] || content.en;
---

<section id="benefits" class="py-24 relative overflow-hidden">
  <div
    class="absolute inset-0 bg-gradient-to-r from-purple-50 to-pink-50 pointer-events-none"
  >
  </div>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative">
    <div class="text-center mb-16">
      <p class="text-lg text-gray-600">
        {t.subtitle}
      </p>
    </div>

    <WhyChooseJolero lang={lang} isGrid={true} headingLevel="h2" />
  </div>
</section>
